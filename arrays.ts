const carMakers = ["ford", "toyota", "volvo"];
// we dont really need annotation when we are putting the values into the arrays

// however in a situation where we have an empty array, we need to annotate it
const student_ages: number[] = [];
